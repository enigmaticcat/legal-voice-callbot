# Evaluation dependencies
# Install: pip install -r requirements_eval.txt

# RAGAS evaluation framework (v0.2+)
ragas>=0.2.0

# LangChain + local Qwen and local embeddings
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-huggingface>=0.1.0

# Already in brain/requirements.txt (listed here for standalone venv setup):
sentence-transformers>=3.0.0
qdrant-client>=1.7.0
python-dotenv>=1.0.0

# ASR (for eval_pipeline_ragas.py)
sherpa-onnx>=1.10.0
numpy>=1.24.0
