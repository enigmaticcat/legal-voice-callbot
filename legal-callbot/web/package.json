{
    "name": "legal-callbot-web",
    "version": "0.1.0",
    "private": true,
    "description": "Legal CallBot — Web Frontend",
    "scripts": {
        "dev": "npx -y vite --host 0.0.0.0 --port 3000",
        "build": "npx -y vite build",
        "preview": "npx -y vite preview"
    },
    "dependencies": {
        "react": "^18.3.1",
        "react-dom": "^18.3.1"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.3.1",
        "vite": "^5.4.0"
    }
}